## Jupyter Notebooks

This directory contains Jupyter notebooks with worked examples for using `OutLines`.
Uses demonstrated in these notebooks include :

+ generating mock line profiles to explore the effects of various assumptions or scenarios on emergent spectral features
+ modeling observed line profiles
+ inferring properties from line profiles
